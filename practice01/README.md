# 高级数据库系统技术第 01 次自主实践

## 使用方法

1. 安装 Docker：

   ```shell
   curl -fsSL https://get.docker.com | bash -s -- --mirror Aliyun
   ```
2. 复制 `student.env.template` 文件为 `student.env`，并填入你的姓名和学号。
3. 运行 `run.sh`。
